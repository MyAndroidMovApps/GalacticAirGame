package com.epnfis.gameapplication;

import android.graphics.Bitmap;

public class EnemyPlane extends Sprite {
    public EnemyPlane(Bitmap bitmap, int x, int y) {
        super(bitmap, x, y);
    }
}
